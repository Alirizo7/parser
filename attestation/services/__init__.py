"""Сервисный слой пайплайна аттестации.

Модули намеренно НЕ зависят от Django (только стандартная библиотека и
python-docx), чтобы их было легко тестировать и переносить в Celery-задачу:

    unpack    — распаковка zip (robust к кириллическим именам, фильтр мусора)
    convert   — обёртка над LibreOffice (.doc → .docx)
    normalize — нормализация «грязных» значений (числа, ҳа/йўқ, единицы)
    mapping   — декларативный конфиг якорей извлечения
    extract   — движок извлечения карт и «Перечня» → записи рабочих мест
    pipeline  — оркестрация unpack → convert → extract → датасет
"""
