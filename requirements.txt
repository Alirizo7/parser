# Базовые зависимости проекта «Авто-аттестация»
Django==5.1.4
python-docx==1.2.0
celery==5.4.0
redis==5.2.1
# Прод-сервер (используется в Docker-образе web)
gunicorn==23.0.0
# Драйвер PostgreSQL (прод); в dev по умолчанию SQLite и psycopg не требуется
psycopg[binary]==3.2.9
# Раздача статики без отдельного nginx (удобно для MVP)
whitenoise==6.8.2
